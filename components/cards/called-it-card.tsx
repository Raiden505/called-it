import type { CalledItCardView } from "@/lib/cards/queries";
import { CountryFlag, PlayerAvatar, ProfileAvatar, TeamCrest } from "@/components/media/media";

export function CalledItCard({ card }: { card: CalledItCardView }) { const matchDate = new Intl.DateTimeFormat("en", { day: "numeric", month: "short", year: "numeric" }).format(new Date(card.kickoffAt)); return <article className="ticket-rail overflow-hidden rounded-2xl border border-[var(--lime)]/40 bg-[var(--surface-2)] pt-8 text-[var(--text)] shadow-xl shadow-black/20"><div className="flex items-center justify-between bg-[var(--lime)] px-5 py-3 text-[var(--canvas)]"><span className="font-display text-xl font-bold tracking-[0.04em]">CALLED IT</span><span className="rounded-full border border-[var(--canvas)]/25 px-3 py-1 text-[10px] font-bold uppercase tracking-[0.12em]">{card.rarity}</span></div><div className="p-5 sm:p-7"><div className="flex items-start justify-between gap-4 text-sm"><div><p className="font-bold">{card.tournamentName}</p><p className="mt-1 text-xs text-[var(--text-muted)]">{card.stage} · {matchDate}</p></div><p className="text-right text-[10px] font-bold uppercase tracking-[0.12em] text-[var(--success)]">● Verified<br />prediction</p></div><div className="mt-9 grid grid-cols-[1fr_auto_1fr] items-center gap-3 text-center"><div><TeamCrest name={card.homeTeam.name} code={card.homeTeam.short_name} badgeUrl={card.homeTeam.badge_url} size={36} className="mx-auto" /><p className="mt-2 font-bold">{card.homeTeam.short_name}</p><CountryFlag countryCode={card.homeTeam.country_code} label={card.homeTeam.name} /></div><div><p className="font-display text-6xl font-bold leading-none text-[var(--lime)]">{card.actualHomeScore}–{card.actualAwayScore}</p><p className="mt-2 text-[10px] font-bold uppercase tracking-[0.14em] text-[var(--text-muted)]">Exact score</p></div><div><TeamCrest name={card.awayTeam.name} code={card.awayTeam.short_name} badgeUrl={card.awayTeam.badge_url} size={36} className="mx-auto" /><p className="mt-2 font-bold">{card.awayTeam.short_name}</p><CountryFlag countryCode={card.awayTeam.country_code} label={card.awayTeam.name} /></div></div><div className="mt-8 grid grid-cols-2 divide-x divide-[var(--line)] border-y border-[var(--line)]"><div className="py-4 pr-4"><p className="text-[10px] font-bold uppercase tracking-[0.14em] text-[var(--text-faint)]">First scorer</p><div className="mt-2 flex items-center gap-2"><PlayerAvatar name={card.firstGoalscorerName} photoUrl={card.firstGoalscorerPhotoUrl} size={28} /><p className="text-sm font-bold">{card.firstGoalscorerName}</p></div></div><div className="py-4 pl-4"><p className="text-[10px] font-bold uppercase tracking-[0.14em] text-[var(--text-faint)]">Confidence</p><p className="mt-2 text-sm font-bold text-[var(--lime)]">{card.confidenceMultiplier}× call</p></div></div><div className="mt-5 flex items-center justify-between gap-4 text-xs font-bold"><div className="flex items-center gap-2 text-[var(--text-muted)]"><ProfileAvatar name={card.predictor.displayName} avatarUrl={card.predictor.avatarUrl} size={28} /><span>{card.predictor.displayName}</span><CountryFlag countryCode={card.predictor.countryCode} label="Predictor country" /></div><p className="text-right text-[var(--lime)]">Rarer than {Math.max(0, 100 - card.rarityPercentage).toFixed(1)}%</p></div></div></article>; }
