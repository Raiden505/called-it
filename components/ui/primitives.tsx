export function PageHeader({ eyebrow, title, description, action }: { eyebrow: string; title: string; description?: string; action?: React.ReactNode }) { return <header className="flex flex-col justify-between gap-5 border-b border-[var(--line)] pb-7 sm:flex-row sm:items-end"><div><p className="text-xs font-bold uppercase tracking-[0.2em] text-[var(--lime)]">{eyebrow}</p><h1 className="font-display mt-2 text-5xl font-bold leading-none tracking-[-0.02em] text-[var(--text)] sm:text-6xl">{title}</h1>{description && <p className="mt-4 max-w-2xl text-sm leading-7 text-[var(--text-muted)]">{description}</p>}</div>{action}</header>; }
export function SectionLabel({ children }: { children: React.ReactNode }) { return <p className="text-xs font-bold uppercase tracking-[0.2em] text-[var(--text-faint)]">{children}</p>; }
export function Button({ children, variant = "primary", type = "button", className = "", disabled = false, onClick }: { children: React.ReactNode; variant?: "primary" | "secondary" | "ghost" | "danger"; type?: "button" | "submit" | "reset"; className?: string; disabled?: boolean; onClick?: () => void }) { const variants = { primary: "bg-[var(--lime)] text-[var(--canvas)] hover:bg-[var(--lime-strong)]", secondary: "border border-[var(--line)] bg-[var(--surface-2)] text-[var(--text)] hover:border-[var(--lime)]", ghost: "text-[var(--text-muted)] hover:bg-[var(--surface-2)] hover:text-[var(--text)]", danger: "border border-[var(--danger)]/40 bg-[var(--danger)]/10 text-[var(--danger)] hover:bg-[var(--danger)]/20" }; return <button className={`inline-flex min-h-11 items-center justify-center rounded-xl px-4 py-2.5 text-sm font-bold transition disabled:cursor-not-allowed disabled:opacity-45 ${variants[variant]} ${className}`} disabled={disabled} onClick={onClick} type={type}>{children}</button>; }
export function Notice({ children, tone = "neutral", role }: { children: React.ReactNode; tone?: "neutral" | "success" | "warning" | "danger"; role?: "alert" | "status" }) { const tones = { neutral: "border-[var(--line)] bg-[var(--surface-2)] text-[var(--text-muted)]", success: "border-[var(--success)]/40 bg-[var(--success)]/10 text-[var(--success)]", warning: "border-[var(--warning)]/40 bg-[var(--warning)]/10 text-[var(--warning)]", danger: "border-[var(--danger)]/40 bg-[var(--danger)]/10 text-[var(--danger)]" }; return <div className={`rounded-xl border px-4 py-3 text-sm leading-6 ${tones[tone]}`} role={role ?? (tone === "danger" ? "alert" : undefined)}>{children}</div>; }
export function EmptyState({ eyebrow = "Nothing here yet", title, description, action }: { eyebrow?: string; title: string; description: string; action?: React.ReactNode }) { return <div className="surface rounded-2xl border-dashed px-6 py-12 text-center"><p className="text-xs font-bold uppercase tracking-[0.18em] text-[var(--lime)]">{eyebrow}</p><h2 className="font-display mt-3 text-3xl font-bold text-[var(--text)]">{title}</h2><p className="mx-auto mt-3 max-w-md text-sm leading-6 text-[var(--text-muted)]">{description}</p>{action && <div className="mt-6">{action}</div>}</div>; }
